<br>
<br>
<br>
<h1>My Tasks</h1>

<div .card>
  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to(task.title, task_path(task)) %>
      <%= link_to "Delete",
              task_path(task),
              method: :delete,
              data: { confirm: "Are you sure?" } %></p>
      </li>
    <% end %>
  </ul>
</div>

<%= link_to "Create",  new_task_path %>
